<Page x:Class="UP1.Views.AuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" 
      d:DesignWidth="1100"
      Title="AuthorPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Мои опубликованные книги" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <Button Content="➕ Добавить новую книгу" 
                    Margin="30,0,0,0" Padding="15,8"
                    Background="#FF99BDB5" Foreground="Black" FontSize="15"
                    Click="BtnAddNewBook_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Foreground="#FF2E544C">
            <WrapPanel x:Name="authorBooksPanel" ItemWidth="180" ItemHeight="260" 
                       Margin="10" HorizontalAlignment="Left"/>
        </ScrollViewer>
    </Grid>
</Page>